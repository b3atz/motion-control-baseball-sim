{
    "name": "motion-control-baseball-sim",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "fresh": "docker-compose down -v && docker-compose build --no-cache && docker-compose up"
    }
}